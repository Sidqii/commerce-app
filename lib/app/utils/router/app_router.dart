import 'package:go_router/go_router.dart';
import 'package:suaka_niaga/app/utils/router/catalog_routes.dart';

final appRouter = GoRouter(
  initialLocation: '/catalog',
  routes: [
    catalogRoute,
    // next features
    // next features
    // next features
    // next features
  ],
);
